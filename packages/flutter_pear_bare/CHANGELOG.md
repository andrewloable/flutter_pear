## 0.4.3

No code or behaviour change to this package. Version bump to stay in
lockstep with `flutter_pear` 0.4.3, which fixes a message-loss bug entirely
inside `pear-end/index.js` (the JS worklet) and its bundle -- nothing in
`flutter_pear_bare`'s native plugin code was touched.

