## 0.4.2

No code or behaviour change. Version bump to stay in lockstep with
`flutter_pear` 0.4.2, which refreshes the pear-end JS dependencies and fixes a
corrupt repacked BareKit iOS xcframework. The in-memory fake's behaviour is
unaffected.

